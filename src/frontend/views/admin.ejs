<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pet Clinic - Admin Panel</title>
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">
</head>

<body>
    <nav class="navbar">
        <h1>🛡️ Admin Panel</h1>
        <div class="user-info">
            <span class="badge-admin">ADMINISTRATOR</span>
            <span>
                <%= userEmail %>
            </span>
            <a href="/logout" class="btn-logout">Logout</a>
        </div>
    </nav>

    <div class="container">
        <!-- Info Box -->
        <div class="info-box">
            <h3>⚠️ Administrative Access Only</h3>
            <p>
                You are logged in as an administrator. Search for appointments by customer email,
                then copy the appointment ID to charge the appointment.
            </p>
        </div>

        <!-- Search Appointments Section -->
        <div class="section">
            <h2>🔍 Search Appointments</h2>
            <div class="search-container">
                <div class="form-group">
                    <label for="searchEmail">Customer Email</label>
                    <div class="search-input-group">
                        <input type="email" id="searchEmail"
                            placeholder="Enter customer email (e.g., john@example.com)">
                        <button type="button" class="btn-search" onclick="searchAppointments()">Search</button>
                    </div>
                </div>
            </div>

            <!-- Search Results -->
            <div id="searchResults" style="display: none;">
                <h3 style="margin: 20px 0 15px; color: #dc3545;">Search Results</h3>
                <div id="appointmentsList"></div>
            </div>

            <!-- Loading spinner -->
            <div id="loadingSpinner" style="display: none; text-align: center; padding: 20px;">
                <div class="spinner"></div>
                <p>Searching...</p>
            </div>
        </div>

        <!-- Charge Appointment Form -->
        <div class="section">
            <h2>💳 Charge Appointment</h2>

            <form action="/admin/charge" method="POST">
                <div class="form-group">
                    <label for="appointmentId">Appointment ID</label>
                    <input type="text" id="appointmentId" name="appointmentId" required
                        placeholder="Enter or paste appointment ID" pattern="[a-zA-Z0-9\-]+">
                    <small style="color: #666; display: block; margin-top: 5px;">
                        Search for appointments above and click "Copy ID" to paste here
                    </small>
                </div>

                <button type="submit" class="btn-charge">
                    Process Charge
                </button>
            </form>
        </div>

        <!-- Admin Features Info -->
        <div class="section">
            <h2>📋 Admin Features</h2>
            <ul class="feature-list">
                <li><strong>Search Appointments:</strong> Find appointments by customer email</li>
                <li><strong>Charge Appointments:</strong> Process billing for completed appointments</li>
                <li><strong>Appointment Management:</strong> Access to all appointment records</li>
                <li><strong>System Administration:</strong> Full control over clinic operations</li>
            </ul>
        </div>
    </div>

    <script>
        window.flashMessage = <% - JSON.stringify(flash || {}) %>;
    </script>
    <script src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
    <script src="/js/admin.js"></script>
</body>

</html>